# ============================================
# [PROJECT_NAME] — Frontend Dockerfile (DESARROLLO)
# ============================================
# Vite dev server con Hot Module Replacement (HMR)
# Los archivos se montan como volumen desde docker-compose.dev.yml

FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3001
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3001"]
